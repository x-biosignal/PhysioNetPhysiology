% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tds-surrogate.R
\name{tdsSurrogateThreshold}
\alias{tdsSurrogateThreshold}
\title{Surrogate significance threshold for TDS links}
\usage{
tdsSurrogateThreshold(X, n_surrogates = 50, quantile = 0.95, ...)
}
\arguments{
\item{X}{Node matrix (or any input accepted by \code{\link[=timeDelayStability]{timeDelayStability()}}).}

\item{n_surrogates}{Number of surrogate datasets (default 50).}

\item{quantile}{Quantile of the surrogate TDS distribution to use as the
threshold (default 0.95).}

\item{...}{Windowing / stability arguments passed to \code{\link[=timeDelayStability]{timeDelayStability()}}.}
}
\value{
A single numeric TDS percentage threshold, with attribute
\code{"surrogate_tds"} holding the pooled surrogate values.
}
\description{
Generates \code{n_surrogates} phase-randomised surrogate datasets (each column
randomised independently, breaking cross-column timing while preserving each
column's power spectrum), recomputes the TDS matrix for each, and returns a
high quantile of the pooled off-diagonal surrogate TDS values. Links in the
real network exceeding this threshold are unlikely to arise from matched
spectra alone.
}
\examples{
set.seed(1)
n <- 2000
x <- as.numeric(stats::filter(rnorm(n), rep(1/5, 5), sides = 2)); x[is.na(x)] <- 0
y <- c(rep(0, 5), x[seq_len(n - 5)]) + rnorm(n, sd = 0.3)
X <- cbind(x = x, y = y)
thr <- tdsSurrogateThreshold(X, n_surrogates = 20,
                             window = 100, step = 50, max_lag = 20,
                             min_stable = 3)
thr

}
