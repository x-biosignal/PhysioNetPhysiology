% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tds-network.R
\name{tdsNetwork}
\alias{tdsNetwork}
\title{Build an organ-interaction network from TDS}
\usage{
tdsNetwork(
  x,
  threshold = NULL,
  surrogate = FALSE,
  X = NULL,
  n_surrogates = 50,
  quantile = 0.95
)
}
\arguments{
\item{x}{A \code{"tds"} object from \code{\link[=timeDelayStability]{timeDelayStability()}}, or a symmetric numeric
TDS matrix.}

\item{threshold}{Minimum TDS percentage for an edge to be kept. If \code{NULL}
(default) and \code{surrogate = TRUE}, a surrogate threshold is computed with
\code{\link[=tdsSurrogateThreshold]{tdsSurrogateThreshold()}}; otherwise a value must be supplied.}

\item{surrogate}{Logical; if \code{TRUE} and \code{threshold} is \code{NULL}, derive the
threshold from phase-randomised surrogates. Requires \code{x} to be a \code{"tds"}
object (so the underlying series and windowing are available) — supply the
original node matrix via \code{X}.}

\item{X}{Original node matrix, required when \code{surrogate = TRUE}.}

\item{n_surrogates, quantile}{Passed to \code{\link[=tdsSurrogateThreshold]{tdsSurrogateThreshold()}}.}
}
\value{
An object of class \code{"tds_network"}: a list with \code{adjacency} (weighted,
thresholded), \code{threshold}, \code{nodes}, \code{degree} (number of links per node),
\code{strength} (summed edge weight per node), and \code{edges} (a data frame of the
surviving links).
}
\description{
Thresholds a TDS matrix into a weighted, undirected network: nodes are
physiological systems and edges are stable dynamic couplings whose weight is
the TDS percentage. Edges below \code{threshold} are removed.
}
\examples{
set.seed(1)
n <- 3000
x <- as.numeric(stats::filter(rnorm(n), rep(1/5, 5), sides = 2)); x[is.na(x)] <- 0
y <- c(rep(0, 5), x[seq_len(n - 5)]) + rnorm(n, sd = 0.3)
z <- rnorm(n)
X <- cbind(brain = x, heart = y, muscle = z)
res <- timeDelayStability(X, window = 100, step = 50, max_lag = 20, min_stable = 3)
net <- tdsNetwork(res, threshold = 40)
net$edges

}
