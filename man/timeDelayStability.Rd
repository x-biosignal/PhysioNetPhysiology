% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tds.R
\name{timeDelayStability}
\alias{timeDelayStability}
\title{Time Delay Stability across a multivariate physiological series}
\usage{
timeDelayStability(
  X,
  sampling_rate = 1,
  window = NULL,
  step = NULL,
  max_lag = NULL,
  window_sec = NULL,
  step_sec = NULL,
  max_lag_sec = NULL,
  stability_tol = 1L,
  min_stable = 4L,
  nodes = NULL
)
}
\arguments{
\item{X}{A numeric matrix or data frame, time (rows) by nodes (columns), or an
object accepted by \code{\link[=physioNodeMatrix]{physioNodeMatrix()}} (named list of equal-length vectors).}

\item{sampling_rate}{Sampling rate in Hz of the rows of \code{X} (default 1).}

\item{window, step, max_lag}{Window length, step, and maximum lag in \strong{samples}
(override the \verb{*_sec} arguments when supplied).}

\item{window_sec, step_sec, max_lag_sec}{The same quantities in \strong{seconds}.
Defaults: \code{window_sec = 60}, \code{step = window/2}, \code{max_lag = window/4}.}

\item{stability_tol}{Maximum change in optimal lag (in samples) between
consecutive windows for the pair to be considered stable (default 1).}

\item{min_stable}{Minimum number of consecutive windows forming a stable
period (default 4).}

\item{nodes}{Optional character vector of node names (defaults to the column
names of \code{X}).}
}
\value{
An object of class \code{"tds"}: a list with \code{tds} (nodes x nodes TDS
percentage matrix, diagonal 0), \code{stable_lag} (nodes x nodes dominant stable
lag in samples), \code{pairs} (per-pair detail incl. \code{tau0} and \code{in_stable}),
\code{nodes}, \code{n_windows}, and \code{params}.
}
\description{
Computes pairwise Time Delay Stability (TDS) for every pair of columns in
\code{X}. Each column is treated as one physiological node (e.g. an EEG band-power,
heart rate, respiration, an EMG envelope). The result is a symmetric matrix
of TDS strengths (percentage of time each pair is stably coupled).
}
\details{
Windowing may be given in samples (\code{window}, \code{step}, \code{max_lag}) or in seconds
(\code{window_sec}, \code{step_sec}, \code{max_lag_sec}, using \code{sampling_rate}). Sample
arguments take precedence. Sensible defaults derive \code{step} and \code{max_lag} from
the window when unspecified.
}
\examples{
set.seed(1)
n <- 3000
x <- as.numeric(stats::filter(rnorm(n), rep(1/5, 5), sides = 2))
x[is.na(x)] <- 0
y <- c(rep(0, 5), x[seq_len(n - 5)]) + rnorm(n, sd = 0.3)  # y lags x by 5
z <- rnorm(n)                                              # unrelated
X <- cbind(x = x, y = y, z = z)
res <- timeDelayStability(X, window = 100, step = 50, max_lag = 20,
                          min_stable = 3)
res$tds

}
\references{
Bashan et al. (2012) \doi{10.1038/ncomms1705}.
}
