% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nodes.R
\name{physioNodeMatrix}
\alias{physioNodeMatrix}
\title{Assemble a physiological node matrix}
\usage{
physioNodeMatrix(
  x,
  nodes = NULL,
  standardize = FALSE,
  assay = NULL,
  channels = NULL,
  feature = c("raw", "envelope"),
  target_rate = NULL
)
}
\arguments{
\item{x}{A named list of equal-length numeric vectors, a numeric matrix
(time x nodes), a data frame of numeric columns, or a \code{PhysioExperiment} /
\code{MultiPhysioExperiment} object (channels/modalities become nodes).}

\item{nodes}{Optional character vector of node names (defaults to the names of
\code{x}).}

\item{standardize}{If \code{TRUE}, z-normalise each column (default \code{FALSE}; TDS
z-normalises per window internally, so this is usually unnecessary).}

\item{assay}{For a \code{PhysioExperiment}, which assay to read (default: its
default assay).}

\item{channels}{For a \code{PhysioExperiment}, an optional subset of channels.}

\item{feature}{Per-channel feature: \code{"raw"} (default) or \code{"envelope"} (the
analytic-signal amplitude — often the right node series for coupling).}

\item{target_rate}{Optional rate (Hz) to down-sample each channel to, by
block-averaging. Required for a \code{MultiPhysioExperiment} whose experiments
have differing sampling rates.}
}
\value{
A numeric matrix, time (rows) by nodes (columns), with column names.
}
\description{
Builds the time-by-nodes matrix that \code{\link[=timeDelayStability]{timeDelayStability()}} consumes, where
each node is one physiological variable (an organ system or a derived feature
such as an EEG band-power, heart rate, respiration amplitude, or an EMG
envelope). Inputs are a named list of equal-length numeric vectors, a numeric
matrix, or a data frame.
}
\details{
In Network Physiology, coupling is assessed between the \emph{dynamics} of these
variables, so nodes are typically instantaneous features sampled on a common,
relatively slow grid (e.g. 1 Hz), not the raw high-rate waveforms. Assemble
such features with the relevant \verb{Physio*} package and pass them here.
}
\examples{
nodes <- list(
  hr   = cumsum(rnorm(500)),
  resp = sin(seq(0, 20 * pi, length.out = 500)),
  emg  = abs(rnorm(500))
)
M <- physioNodeMatrix(nodes)
dim(M)

}
