% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardioresp.R
\name{cardiorespiratoryCoupling}
\alias{cardiorespiratoryCoupling}
\title{Analyse cardiorespiratory coupling}
\usage{
cardiorespiratoryCoupling(
  resp,
  rpeaks,
  sampling_rate,
  resp_band = c(0.1, 0.5),
  max_ratio = 8L
)
}
\arguments{
\item{resp}{Respiration signal.}

\item{rpeaks}{Integer R-peak sample indices (same time base as \code{resp}).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{resp_band}{Respiratory passband in Hz (default \code{c(0.1, 0.5)}).}

\item{max_ratio}{Largest heartbeats-per-breath ratio to consider (default 8).}
}
\value{
An object of class \code{"cardioresp"}: a list with \code{ratio} (a
\code{"heart:resp"} string), \code{n}, \code{m}, \code{lambda} (locking index), \code{sync_index}
(1:1 phase-locking value), \code{synchrogram}, and the \code{table} of scanned ratios.
}
\description{
High-level convenience wrapper: extracts respiratory phase (band-passed
Hilbert) and cardiac phase (from R-peaks), scans n:m phase locking, and
returns the dominant heartbeats-per-breath ratio, its locking index, and the
synchrogram.
}
\examples{
t <- seq(0, 120, by = 0.01); fs <- 100
resp <- sin(2 * pi * 0.25 * t)
card <- sin(2 * pi * 1.0 * t)                       # 4 beats per breath
rpeaks <- which(diff(sign(card)) > 0)
cr <- cardiorespiratoryCoupling(resp, rpeaks, fs)
cr$ratio
}
