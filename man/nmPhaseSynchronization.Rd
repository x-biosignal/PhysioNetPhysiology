% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase-sync.R
\name{nmPhaseSynchronization}
\alias{nmPhaseSynchronization}
\title{Scan n:m phase-locking over a grid of ratios}
\usage{
nmPhaseSynchronization(phi1, phi2, orders_n = 1:8, orders_m = 1:8)
}
\arguments{
\item{phi1, phi2}{Instantaneous phases.}

\item{orders_n, orders_m}{Integer vectors of orders to scan.}
}
\value{
A list with \code{table} (data frame of \code{n}, \code{m}, \code{lambda}), and the
dominant \code{n}, \code{m}, \code{lambda}, and \code{ratio} (a \code{"f1:f2"} string = \code{m:n}).
}
\description{
Computes \eqn{\lambda_{n,m}} for every coprime \verb{(n, m)} in the requested range
and returns the dominant ratio. The frequency ratio \code{f1 : f2} implied by the
winning \verb{(n, m)} is \code{m : n}.
}
\examples{
t <- seq(0, 120, by = 0.01)
resp <- sin(2 * pi * 0.25 * t)
card <- sin(2 * pi * 1.0 * t)                 # 4x faster: locked 4:1
pr <- instantaneousPhase(resp, 100)
pc <- instantaneousPhase(card, 100)
nmPhaseSynchronization(pc, pr)$ratio
}
