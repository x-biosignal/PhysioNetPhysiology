% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cardioresp.R
\name{synchrogram}
\alias{synchrogram}
\title{Cardiorespiratory synchrogram}
\usage{
synchrogram(resp_phase, rpeaks, m = 1L)
}
\arguments{
\item{resp_phase}{Instantaneous respiratory phase (length = n samples), e.g.
from \code{\link[=instantaneousPhase]{instantaneousPhase()}}.}

\item{rpeaks}{Integer R-peak sample indices.}

\item{m}{Number of respiratory cycles to wrap into (default 1).}
}
\value{
A data frame with \code{beat} (R-peak sample index) and \code{psi} (respiratory
phase at that beat, in \verb{[0, m)}), of class \code{"synchrogram"}.
}
\description{
The classic Schafer-Rosenblum synchrogram: the respiratory phase observed at
each heartbeat, wrapped into \code{m} respiratory cycles. Under n:m locking (n
heartbeats per m breaths) the points collapse onto \code{n} horizontal bands.
}
\examples{
t <- seq(0, 120, by = 0.01); fs <- 100
resp_phase <- instantaneousPhase(sin(2 * pi * 0.25 * t), fs)
rpeaks <- which(diff(sign(sin(2 * pi * 1.0 * t))) > 0)   # ~1 Hz beats
sg <- synchrogram(resp_phase, rpeaks, m = 1)
}
