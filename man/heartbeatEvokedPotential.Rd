% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brain-heart.R
\name{heartbeatEvokedPotential}
\alias{heartbeatEvokedPotential}
\title{Heartbeat-evoked potential (HEP)}
\usage{
heartbeatEvokedPotential(
  eeg,
  rpeaks,
  sampling_rate,
  window = c(-0.1, 0.6),
  baseline = c(-0.1, 0)
)
}
\arguments{
\item{eeg}{Numeric brain signal (one channel).}

\item{rpeaks}{Integer R-peak sample indices (same time base as \code{eeg}).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{window}{Epoch window \code{c(pre, post)} in seconds relative to the R-peak
(default \code{c(-0.1, 0.6)}).}

\item{baseline}{Baseline window \code{c(from, to)} in seconds for per-epoch
correction; \code{NULL} to skip (default \code{c(-0.1, 0)}).}
}
\value{
An object of class \code{"hep"}: a list with \code{time} (s, relative to the
R-peak), \code{hep} (averaged waveform), \code{epochs} (epochs x time matrix), \code{n}
(number of epochs), and \code{sampling_rate}.
}
\description{
Epochs a brain signal around each R-peak, baseline-corrects, and averages to
the heartbeat-evoked potential.
}
\examples{
set.seed(1); fs <- 250; n <- 30 * fs
rpeaks <- seq(fs, n - fs, by = round(0.9 * fs))
eeg <- rnorm(n)
hep <- heartbeatEvokedPotential(eeg, rpeaks, fs)
}
