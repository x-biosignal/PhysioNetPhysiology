% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-metrics.R
\name{communityDetection}
\alias{communityDetection}
\title{Detect communities in a physiological network}
\usage{
communityDetection(
  x,
  method = c("louvain", "fast_greedy", "walktrap", "leading_eigen", "infomap"),
  seed = NULL
)
}
\arguments{
\item{x}{A \code{tds_network} (from \code{\link[=tdsNetwork]{tdsNetwork()}}) or a weighted adjacency matrix.}

\item{method}{Community-detection algorithm: \code{"louvain"} (default),
\code{"fast_greedy"}, \code{"walktrap"}, \code{"leading_eigen"}, or \code{"infomap"}.}

\item{seed}{Optional integer seed (some algorithms are stochastic).}
}
\value{
A \code{community_partition} object: \code{membership} (named integer vector),
\code{modularity} (Q), \code{n_communities}, \code{sizes}, and \code{method}.
}
\description{
Partitions a network into communities (modules) and reports the modularity Q.
The membership can be passed to \code{\link[=moduleRoles]{moduleRoles()}} / \code{\link[=participationCoefficient]{participationCoefficient()}},
closing the loop those role metrics need. Delegates to \code{igraph}.
}
\examples{
\donttest{
set.seed(1)
A <- matrix(0, 8, 8)
A[1:4, 1:4] <- 0.9; A[5:8, 5:8] <- 0.9; A[1, 5] <- A[5, 1] <- 0.2
diag(A) <- 0
communityDetection(A)
}
}
\seealso{
\code{\link[=networkMetrics]{networkMetrics()}}, \code{\link[=moduleRoles]{moduleRoles()}}
}
