# Per-layer and overlapping (multiplex) degree

Per-layer and overlapping (multiplex) degree

## Usage

``` r
multiplexDegree(x)
```

## Arguments

- x:

  A `"multiplex"` object.

## Value

A list with `per_layer` (nodes x layers degree matrix) and `overlapping`
(summed degree across layers per node).
